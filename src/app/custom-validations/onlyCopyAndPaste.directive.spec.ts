/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OnlyCopyAndPasteDirective } from './onlyCopyAndPaste.directive';

describe('Directive: OnlyCopyAndPaste', () => {
  it('should create an instance', () => {
    const directive = new OnlyCopyAndPasteDirective();
    expect(directive).toBeTruthy();
  });
});
